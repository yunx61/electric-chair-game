{
  "name": "electric-chair-duel",
  "version": "4.0.0",
  "private": true,
  "description": "Realtime electric chair mind game with online, AI, challenges, achievements and mobile-first one-screen UI",
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "preview": "node ./scripts/preview.mjs",
    "check": "node --check public/app.js && node --check public/service-worker.js && node --check public/js/game/replay.js && node --check public/js/firebase/online-session.js && node --check public/js/ai/local-session.js && node --check scripts/prod-smoke.mjs",
    "test": "node --test test/game.test.js test/static.test.js",
    "test:rules": "firebase emulators:exec --only database \"node --test test/rules.test.js\"",
    "smoke:prod": "node ./scripts/prod-smoke.mjs",
    "verify": "pnpm run build && pnpm run check && pnpm test"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.19.0",
  "dependencies": {
    "firebase": "12.18.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "esbuild": "0.28.2",
    "firebase-tools": "15.28.1"
  }
}
